server:
  port: 8082
spring:
  security:
    user:
      name: hzm
      password: hzm
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/security?characterEncoding=utf-8
mybatis:
  mapper-locations:
    - classpath*:mapper/*.xml
